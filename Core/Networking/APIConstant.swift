import Foundation

enum APIConstants {

    enum Paths {

        static let users =
            "/users"

        static let login =
            "/login"

        static let products =
            "/products"

        static let profile =
            "/profile"
    }
}
